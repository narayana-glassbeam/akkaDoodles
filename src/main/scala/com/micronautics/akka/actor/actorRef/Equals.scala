package com.micronautics.akka.actor.actorRef

import akka.actor.Actor

/** '''ActorRef.equals()'''
  * {{{def equals (that: Any): Boolean}}} */
object Equals extends Actor {
  //this.equals(that)
  
  def receive = {
    case _ =>
  }
}